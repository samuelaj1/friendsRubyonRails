<%= form_with(model: transaction) do |form| %>
  <% if transaction.errors.any? %>
    <div style="color: red">

      <h2><%= pluralize(transaction.errors.count, "error") %> prohibited this transaction from being saved:</h2>

      <ul>
        <% transaction.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>

    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name, class: "form-control" %>
  </div>

  <div>
    <%= form.label :amount, style: "display: block" %>
    <%= form.text_field :amount, class: "form-control", placeholder:"amount" %>
  </div>

<!--  <div>-->
    <%#= form.label :user_id, style: "display: block" %>
    <%#= form.number_field :user_id, id: :transaction_user_id, value:current_user.id, class: "form-control", placeholder:"user_id" %>
<!--  </div>-->

  <div class="form-group">
    <%= form.label :user_id, class: "form-label" %>
    <%= form.collection_select :user_id, @users, :id, :email, { prompt: "Select a user" }, { class: "form-control form-select" } %>
  </div>

  <div>
    <%= form.label :status, style: "display: block" %>
    <%= form.text_field :status, class: "form-control" %>
  </div>

  <div class="actions mt-3">
    <%= form.submit class: "btn btn-primary" %>
  </div>
<% end %>
